process Dump =  {
  #keep the logging output to a nice level
  include "FWCore/MessageLogger/data/MessageLogger.cfi"

  source = PoolSource {
	  untracked vstring fileNames = {
#	'file:/tmp/jjhollar/jpsimm.dimuonskim.root'
#       'file:/tmp/jjhollar/upsilon.dimuons.root' 
       'file:/tmp/jjhollar/zmm.dimuonskim.root' 

	  }
  }

  untracked PSet maxEvents = {untracked int32 input = -1}

  module  hltFilter = hltHighLevel from "HLTrigger/HLTfilters/data/hltHighLevel.cfi" 
  replace hltFilter.TriggerResultsTag = TriggerResults::HLT 
  replace hltFilter.HLTPaths = {"HLT1MuonPrescalePt5"} 

  module plotter = DimuonPlotter {} 

  path p = { hltFilter, plotter }
}
